url: https://postman-echo.com/post?id={{id}}&environment={{environment}}

method: post

header:
  Content-Type: application/json

body: |
  {
      "title": "{{title}}",
      "summary": "{{summary}}"
  }
